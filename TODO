Tabs:
  - We should have one project per window.
  - At file opening, decide if we need to open in new window or new tab
  - Use magic commands for the decision, extra shortcuts as well

App:
  - Add ability to open a new window

Window:
  - Remove the tab mess. Keep track of the root file.

Reorganization:
  - Compilation related things should go back to document manager.
  - Saving is left in sourceview
  - opening in window?

SourceView:
  - Notify user if file changed on disk

Completion:
  - Figure out how it works