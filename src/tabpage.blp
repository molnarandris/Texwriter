using Gtk 4.0;
using Adw 1;

template TabPage {
  Paned paned {
    position: 300;
    resize-start-child: true;
    resize-end-child: true;

    [start]
    .TexwriterSource sourceview {}

  } // Paned
}